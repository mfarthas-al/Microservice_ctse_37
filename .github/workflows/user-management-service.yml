name: User Management Service CI-CD

on:
  push:
    branches:
      - main
      - develop
    paths:
      - services/user-management-service/**
      - .github/workflows/user-management-service.yml
  pull_request:
    branches:
      - main
      - develop
    paths:
      - services/user-management-service/**
      - .github/workflows/user-management-service.yml

jobs:
  ci:
    name: User Management Service Build Validation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: services/user-management-service/package-lock.json

      - name: Install user-management-service dependencies
        run: npm ci --prefix services/user-management-service

      - name: Build user-management-service Docker image
        run: docker build -t user-management-service:ci ./services/user-management-service

  cd:
    name: Publish User Management Service Image
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push user-management-service image
        uses: docker/build-push-action@v6
        with:
          context: ./services/user-management-service
          file: ./services/user-management-service/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/microservice-ctse-37-user-management-service:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/microservice-ctse-37-user-management-service:${{ github.sha }}
